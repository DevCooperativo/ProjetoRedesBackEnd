import styled from "styled-components";

const row = styled.tr`

`
const head = styled.th`
    text-align: center;
    border: 1px solid white;
    padding: 5px 10px;
`
const cell = styled.td`
    text-align: center;
    border: 1px solid white; 
    padding: 5px 10px;
`

export const S = {
    row,
    head,
    cell,
}